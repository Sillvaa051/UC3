1. em que ano o modelo relacional de banco de dados foi desenvolvido?
1970-1972

2. Quem é considerado o "pai dos bancos de dados relacionados?
Edgar frank codd

3.Explique a diferença de um Banco de Dados, um Sistema Gerenciador de Banco de Dados.
O banco de dados e onde armazena os dados, o gerenciador é o software responsável por criar

4. Quais são as principais características de um banco de dados?
organização estruturada dos dados, redução da redundância, integração das informações, consistência, independência

5. Quais são os diferentes níveis de abstração em um banco de dados.
Os níveis de abstração de um banco de dados são: externo, que mostra o que cada usuário precisa ver. conceitual, que mostra a estrutura geral do banco e interno, que mostra como os dados são armazenados fisicamente

6.Liste as vantagens de usar um banco de dados.
Reduzir erros no sistema e maior segurança